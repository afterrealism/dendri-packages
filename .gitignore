# Node / TypeScript
node_modules/
dist/
*.log

# Pack output — regenerated by deploy/bootstrap.sh in a containerized pnpm pack.
# Example apps reference these as `file:` paths, so they must exist at podman
# build time but are never committed.
*.tgz

# Env / secrets
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# AI assistant scratch
.claude/
.cursor/
